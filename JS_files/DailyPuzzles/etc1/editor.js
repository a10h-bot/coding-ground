//* append - Append string to the end of S.
// *delete - Delete the last characters of S.
// *print - Print the character of S.
// *undo - Undo the last(not previously undone) operation of type 1 or 2, reverting S to the state it was in prior to that operation.
let S = "";
//!to appned w at the end of S
function Append(W) {
  return S + W;
}
//!to delete k charctor from S
function Delete(k) {}
//!to print kth charactor of S
function print(k) {}
//!To undo appned and delete
function undo() {}
console.log(Append(""));
